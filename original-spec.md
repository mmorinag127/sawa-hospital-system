---
project: hospital-order-system
feature: 001-order-mvp
status: draft
created_at: 2025-12-23
---

# Feature Specification: 病院・施設 発注FAX（PDF）自動取込〜集計〜ラベル/納品書/総量CSV出力（MVP）

## Summary
施設から送られてくる発注連絡表（FAXをPDF化したもの）をメール添付で受信し、PDFから注文数を抽出してデータ化する。
週次メニュー（Excel）と突合して袋分け計算を行い、以下を自動出力する：
- ラベル印刷用CSV（現行形式互換）
- 納品書Excel（施設×日）
- 製造側の総量CSV（現行形式互換）

運用者がPDF原本を見ながら、Web上で確認・修正し「確定」できることを必須とする。

## Goals
- PDF受信〜抽出〜施設/週推定〜明細化〜袋分け〜出力生成までを自動化する
- OCRが不完全でも、運用者の確認・修正で正しい出力を作れる
- 施設ごとのフォーマット差・ローカルルール差を、施設マスター（設定）で吸収する
- 週次メニューは毎週アップロードし、単位/温冷等をUIで確定できる

## Non-Goals (MVP)
- スマホ対応（PCブラウザのみ）
- 1PDF複数ページの統合処理（発生した場合は “別施設として扱う” 運用で吸収）
- 施設名の表記揺れ辞書（エイリアス）による自動補正（手動選択で解決）

---

## User Scenarios & Testing (mandatory)

### Story 1: メール添付PDFを即時処理し、「要確認」状態で一覧に出す (P1)
As an 運用者,
I want 転送メールに添付されたPDFが届いたら、システムが即時処理して一覧に出してほしい,
So that 施設数が増えても手入力を増やさず運用できる。

Acceptance Criteria
1. 添付PDFを受信すると、注文（施設×週）レコードが生成され、PDFが保存される。
2. OCR/抽出が走り、明細（slotごとの数量）が生成される。
3. 施設名が自動確定できない場合、注文は「施設未確定」として一覧に出る（運用者がプルダウンで確定できる）。
4. 同一施設×同一週の注文PDFが既に存在する場合、新しいPDFを採用し、古い注文は出力対象外となる。

Test Notes
- 施設ごとに列構成が異なるPDFを複数投入しても、最低限「要確認」まで到達すること。

### Story 2: PDF原本を見ながら抽出結果を修正し、確定すると出力が自動更新される (P1)
As an 運用者,
I want PDFビューアで原本を見ながら明細を編集し「確定」できる,
So that OCR精度が十分でなくても運用できる。

Acceptance Criteria
1. 明細のセル編集はインラインで行える。保存は「確定」押下時のみ反映される。
2. 修正された数値が正となり、OCR由来の値は出力に使わない。
3. 確定後に、ラベルCSV・納品書Excel・総量CSVが自動更新される。
4. 週ID/対象週は運用者が変更でき、変更後の週で再計算・再出力される。

### Story 3: 週次メニューをアップロードし、単位/温冷等をプルダウンで確定できる (P1)
As an 管理者,
I want 週次メニューExcelをアップロードして、各メニューの単位/温冷/区分をUIで確定したい,
So that 袋分けとラベル表記の入力を安定させたい。

Acceptance Criteria
1. 週次メニューExcelアップロードで「週」と「週のメニュー一覧」が生成される。
2. 管理者はメニュー行ごとに、以下をプルダウン/入力で設定できる：
   - 単位種別（例：g / 切 / 個）
   - 1人前あたり数量（数値）
   - 温菜/冷菜
   - 朝/昼/夕
   - メニュー区分（主菜/主菜①/副菜①/副菜②/主菜添え 等）
3. 設定はその週の袋分け計算・ラベル/納品書出力に反映される。

### Story 4: 施設マスターでテンプレとルールを管理し、新規施設追加に対応できる (P2)
As an 管理者,
I want 新しい施設が増えても、施設マスターを追加・設定することで対応したい,
So that コード変更や開発介入を最小化できる。

Acceptance Criteria
1. 施設マスターにより、以下を設定できる：
   - 施設名
   - 施設内エリア（階/ユニット/通所/職員 等の名称）
   - 発注FAXテンプレ（抽出定義）
   - 袋分けルール（データ定義）
   - ラベル設定（固定文言、冷蔵/冷凍、賞味期限計算、備考反映ポリシー）
   - 納品書テンプレ（Excel）と列マッピング
2. 施設マスター更新により、次回以降の処理・出力に反映される。

---

## Edge Cases
- 施設名抽出が低信頼 / 候補複数 / 0件 → 運用者が施設選択
- OCR失敗 → リトライ（初期3回、設定可能）→ 失敗なら手入力へ
- 「未記入」セル → 0扱い
- 変更欄あり → 変更後の数を正として採用（差分ではない）
- 1PDF複数ページ → MVPでは統合しない（別施設として扱う運用で吸収）
- 禁食・特記事項が自然言語で混入 → 施設マスターのポリシーに従って反映、必要なら注文単位で上書き

---

## Requirements (mandatory)

### Functional Requirements

#### A. Identification & Retention
FR-A1. 主要エンティティは「3文字程度の接頭語＋数値/週表現」で一意IDを付与する。
- 例：施設ID = FAC00001、週ID = WEK2025W14
FR-A2. 受信メールと添付PDFを保持する（初期 1〜2ヶ月、設定可能）。
FR-A3. 同一施設×同一週の注文が複数届いた場合、新しいものを採用し古いものは出力対象外とする。

#### B. Email Ingestion
FR-B1. 転送されてくるメールは全て処理対象（初期は添付ファイルのみ）。
FR-B2. 処理は即時。タイムゾーンはJST。
FR-B3. 対象週はPDF記載から抽出（将来、メール本文参照へ拡張可能）。

#### C. Facility Resolution
FR-C1. 施設名はPDF内の指定領域から抽出する（テンプレで管理）。
FR-C2. 施設エイリアス辞書は持たない。確定できない場合は運用者が選択する。

#### D. Weekly Menu Management
FR-D1. 管理者は週次メニューExcelをアップロードし、週に紐づけて保持できる。
FR-D2. 管理者はメニュー行ごとに単位/1人前量/温冷/区分/時間帯をUIで確定できる。
FR-D3. 週次メニューは原則全施設共通。ただし施設別例外を登録できる（MVPは「置換」だけ対応、他は将来拡張可）。

#### E. OCR & Extraction
FR-E1. OCRは日本語、手書き/印字混在、半角全角混在を許容する。
FR-E2. OCRのリトライ回数は設定可能（初期3回）。
FR-E3. 未記入セルは0とみなす。
FR-E4. 運用者の修正値が正。OCR値は保持しても良いが出力には使わない。
FR-E5. 変更欄（変更①/②）が存在し数値が入っている場合、「変更後の数」を正として採用する（差分ではない）。

#### F. Packaging (袋分け) & Label Generation
FR-F1. 袋分けルールは “施設マスター” でデータ定義し、コード変更なしで更新可能であること。
FR-F2. 袋分けは「施設 × 日付 × 朝昼夕 × メニュー × 食種 × エリア」単位で独立して計算する。
FR-F3. エリアがある施設は必ずエリアごとに袋・ラベルを分ける。
FR-F4. ラベル出力は現行CSV形式互換で出力する（フォーマットは Output Format を参照）。
FR-F5. ラベルCSVは、数量0（0g/0人前）の行は出力しない。
FR-F6. ラベルに印字される情報は、現行シール例の項目セットを踏襲できること：
- 施設名、賞味期限、保存方法、食事区分（朝昼夕・エリア・メニュー区分・必要なら別袋）、温冷、商品名、内容量、内容詳細、所在地、製造者、注意文
FR-F7. 冷蔵/冷凍と賞味期限算出（冷凍=喫食日+3か月、冷蔵=喫食日）は施設マスター設定で切替可能。

#### G. Delivery Note (納品書) Generation
FR-G1. 納品書は「施設×日」単位でExcel出力する。
FR-G2. 「日付/区分/メニュー名」列は全施設で共通。
FR-G3. 数量列の構成（食種×エリア列の並び）は施設ごとに異なるため、施設マスターのテンプレ/マッピング定義に従って出力する。
FR-G4. 納品書テンプレは差し替え可能（将来フォーマット変更に追随）。
FR-G5. 納品書内の手書きメモ領域は自動転記対象外（無視して良い）。

#### H. Manufacturing Aggregate CSV
FR-H1. 製造側総量CSVは現状運用のCSV形式互換で出力する。

#### I. UI / Workflow
FR-I1. PCブラウザのみ対応。
FR-I2. トップページで対象週の進捗（未着/要確認/確定/エラー）を施設別に一覧できる。
FR-I3. 一覧にはフィルタ（未着/完了/エラー/文字列検索）がある。
FR-I4. PDFビューアはズーム必須、ページ送りは原則不要。
FR-I5. 編集はインライン、保存は確定ボタンのみ。Undo不要。
FR-I6. エラーは「次にやること」が分かり、該当修正画面へ直行できる。

#### J. AuthZ / AuthN
FR-J1. ロールは2種類（管理者/運用者）。
FR-J2. 管理者のみ：マスター更新、テンプレ更新、ユーザー管理、ログ閲覧。
FR-J3. 運用者：注文確認、修正、確定、出力取得。
FR-J4. 認証：管理者=Googleアカウント、運用者=ID/パスワードを基本（詳細はplanで決定）。

#### K. Logging & Notifications
FR-K1. 修正作業・アップロード・設定変更はログ化。保持期間は1〜2ヶ月（設定可能）。
FR-K2. 業務エラーは運用者へ、システム致命的エラーは管理者へ通知する。

#### L. Non-Functional
FR-L1. 1日最大100施設を想定。
FR-L2. ダウンタイム後は未処理メールを再処理し追いつける。
FR-L3. セキュリティは厳格に（アクセス制御、暗号化、監査）。詳細はplanで具体化。

---

## Output Format: Label CSV (現行互換)
- エンコーディング: [TBD in plan]（現行に合わせる。一般にCP932想定）
- 既存の「ラベル印刷機に渡すCSV」と同一の列構成・順序で出力する。
- 0行（数量0）は出力しない。

(列定義の厳密化は plan/tasks で既存CSVに合わせて固定する)

---

## Key Entities
- Facility
- FacilityArea (エリア/階/ユニット/通所/職員 等)
- FacilityMasterConfig
  - FaxTemplate
  - PackagingPolicy
  - LabelProfile (冷蔵/冷凍、賞味期限、備考反映ポリシー、固定文言)
  - InvoiceTemplate (Excel) + ColumnMapping
- WeeklyMenu
- MenuItem (単位/1人前量/温冷/朝昼夕/区分)
- OrderDocument (受信PDF)
- Order (施設×週)
- OrderLine (日付×区分×メニュー×食種×エリア×数量)
- Bag (袋分け結果)
- LabelRow (ラベルCSV 1行)
- DeliveryNote (納品書Excel)
- ManufacturingAggregateRow (総量CSV 1行)

---

## Success Criteria (mandatory)
SC1. 運用者が手入力でゼロから転記する作業を原則なくす（確認・修正に限定）。
SC2. 受信PDFが「要確認一覧」に反映されるまでが実用的な時間である。
SC3. エラーは運用者がUIのみで復旧できる（管理者呼び出しを最小化）。
SC4. 確定に追随してラベルCSV・納品書・総量CSVが自動更新され、印刷運用が成立する。
